This is basically the same as nix-shell-using-pip, but you can't pass the python version as a parameter (there are upsides to that!), and instead of typing:
```bash
nix-shell
```
to use it, you type
```bash
nix develop
```